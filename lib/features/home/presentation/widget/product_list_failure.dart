import 'package:flutter/material.dart';

class ProductListFailure extends StatelessWidget {
  const ProductListFailure({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
